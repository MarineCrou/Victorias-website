{{#if @site.members_enabled}}
  {{#unless @member}}
    <section class="gh-cta gh-outer">
      <div class="gh-cta-inner gh-inner">
        {{#if title}}
          <p class="gh-cta-title">{{title}}</p>
        {{/if}}
        <button class="gh-cta-input" data-portal>jamie@example.com
          <span>S'inscrire</span></button>
      </div>
    </section>
  {{/unless}}
{{/if}}